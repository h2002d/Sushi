@model List<LaserArt.Models.CardModel>

@{
    ViewBag.Title = "Корзина";
}

<div class="jumbotron">
    <h3>Զամբյուղ</h3>
    <table>
        <tr class="card-details-header">
            <th>Անվանում</th>
            <th>Քանակ</th>
            <th>Գին</th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {

            <tr id="@(item.product.Id+"itemCard")">
                <td>

                    <p>@item.product.ProductTitle</p>
                    <img src="@item.product.ImageSource" class="imageHeader" />
                </td>

                <td>
                    <p>@item.ProductQuantity</p>
                </td>
                <td>
                    <p>@item.product.Price руб.</p>
                    @if (@item.product.PriceDiscounted != 0)
                {
                    <del> @item.product.PriceDiscounted</del>
                    }
                </td>
                <td>
                    <a class="removeCard" id="@item.product.Id">Удалить</a>
                </td>
            </tr>
        }


        <tr>
            <td></td>
            <td></td>
            <td><p>Сумма заказа:@ViewBag.Sum</p>
            <td>
                @if (Model.Count > 0)
                {
                    <input type="button" class="btn btn-default" onclick="orderBtnClick()" value="Оформить заказ" />
                }
            </td>
        </tr>

    </table>
</div>
